# نسخ إلى .env.production أو .env عند بناء الواجهة للإنتاج
# عنوان الـ API العام (Render) — يجب أن ينتهي بـ /api أو بدون، حسب ما تضعه في auth.js
# مثال: VITE_API_BASE_URL=https://radar-backend.onrender.com/api
# VITE_API_BASE_URL=
